services:
  - type: web
    name: eds-analyser-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: EDS_ANALYSER_DB
        value: /app/.data/eds-analyser.sqlite
